name: Semgrep pipeline 🔎

run-name: "Semgrep Security Scan - ${{ github.event_name }} by ${{ github.actor }}"

on:
  push:
    branches: [main, dev, uat]
  pull_request:
    branches: [main, dev, uat]

jobs:
  semgrep:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Install Semgrep
        run: pip install semgrep

      - name: Run Semgrep
        run: semgrep --config auto --error

      - name: Label PR on security findings
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              labels: ['security-findings', 'semgrep-failed', 'semgrep']
            });

            // Add comment with details
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              body: '⚠️ **Semgrep Security Scan Failed**\n\nSecurity issues were detected. Please review the workflow logs and address the findings.'
            });

      - name: Label PR on success
        if: success() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              labels: ['security-passed', 'semgrep']
            });
